SET FOREIGN_KEY_CHECKS=0;
TRUNCATE `artifact1`;
TRUNCATE `artifact1_volume`; 
TRUNCATE `artifact2`; 
TRUNCATE `artifact2_volume`; 
-- TRUNCATE `artifact_sequence`;
TRUNCATE `badfile`; 
-- TRUNCATE `dwara_sequences`; 
UPDATE `dwara_sequences` SET `current_val`='0' WHERE `primary_key_fields`='job_id';
TRUNCATE `file1`; 
TRUNCATE `file1_volume`; 
TRUNCATE `file2`; 
TRUNCATE `file2_volume`; 
-- TRUNCATE `file_sequence`;
TRUNCATE `job`; 
TRUNCATE `jobrun`; 
TRUNCATE `processingfailure`; 
TRUNCATE `request`; 
TRUNCATE `t_activedevice`;
TRUNCATE `t_file_job`;
SET FOREIGN_KEY_CHECKS=1;