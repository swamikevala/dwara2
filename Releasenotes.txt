3rd Aug -
	SelectedStorageJob fields updated consistently
	Format - 
		label tmp file is .xml
	ingest
		volumes in a group to be used in order of the barcode
	verify - 
		Filevolume verified date updated
		Only if checksum supports streaming then do on the fly verification else do the bru way...
	Restore and Verification logic is based on restore_verify DB field OR verify userrequest parameter
		when restore happens have the part file named ".restoring" - Not possible not in our control. Bru does, we just feed the destPath and the filetoBeRestored  
	Finalize
		Volumeindex is now written to tape...
		compress the index file - Pending
need to add the following in dwara.properties in testenv
	checksumType=SHA-256
	checksumTypeSupportsStreamingVerification=true

17th July - Process intro, Checksum gen is now a process - Tape integration still in WIP
	upgrade steps
		drop database
		create database
		run the dwara-master sql script
		run the dwara-testmock sql script
	import new projects into eclipse
	use the testcases in dwara-service
	
13th July - Tape mech with mocks for dev env intro and sample integration - Full integration WIP
	upgrade steps
		drop database
		create database
		run the dwara-master sql script
		run the dwara-testmock sql script
	use the testcases in dwara-service
	 

12th July - Format and mapdrives and finalize flow completed - Still some doubts/gaps and TODOs
	Not much change in schema
	
10th July - Verify flow completed
	Drop transaction tables
		drop table request, job, artifact1, file1, artifact2, file2, artifact1_volume, file1_volume, artifact2_volume, file2_volume;
		
8th July - Restore flow completed

6th July - Verify and restore - WIP
	
5th July - FileVolume and ArtifactVolume block fixes, some DB configuration updates
	Use mvn clean install -DskipTests instead of using the skiptests checkbox...
	upgrade steps
		drop database
		create database
		run the dwara-master sql script again
	Execute the dwara-service JobCreator/JobManager tests and check if the filevolume and artifact volume blocks are ok for both tar and bru
	Log extractor - Dev tool
	
1st July - FileVolume and ArtifactVolume Domain specific dynamism
	upgrade steps
		drop database
		create database
		run the dwara-master sql script again
		
29th Jun - More fixes

27th and 28th Jun
	schema changes
		drop database
		create database
		run the dwara-master sql script again
	DB unit intro
		org.ishafoundation.dwaraapi.job.JobCreator_Ingest_Test.assertJobTable() has some sample DBUnit integration. 
		More DBunit integration examples can be found in the other branch
		https://bitbucket.org/ishafoundation/dwara2/src/development/src/test/java/org/ishafoundation/dwaraapi/dbunittests/storage/storagetype/tape/format/FormatTests.java
		https://bitbucket.org/ishafoundation/dwara2/src/development/src/test/java/org/ishafoundation/dwaraapi/dbunittests/storage/storagetype/tape/library/TapeDriveMapperTests.java